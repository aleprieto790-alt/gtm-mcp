---
description: Alias for /launch — run the full pipeline from input to SmartLead campaign
argument-hint: "[website/document/description]"
---

# /leadgen $ARGUMENTS

This is an alias for `/launch`. See `/launch` for the full pipeline documentation.

Run `/launch $ARGUMENTS` with the same arguments.
